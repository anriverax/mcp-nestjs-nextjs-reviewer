export const MARKDOWN_OUTPUT_PROMPT = `
Format the response strictly in Markdown.

Use:
- Clear section headings (##, ###)
- Bullet points
- Code blocks with proper language identifiers
- Short, concise paragraphs

The output must be readable inside VS Code.
`;
